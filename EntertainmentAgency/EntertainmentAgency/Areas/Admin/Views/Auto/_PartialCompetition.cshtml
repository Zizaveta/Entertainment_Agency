@model List<EntertainmentAgency.Models.Competition>

<select name="Competition" class="custom-select" id="SelectedType">
    @for (int i = 0; i < Model.Count; i++)
    {
        <option value=@Model[i].Id>@Model[i].Name</option>
    }
</select>
